#include "Keyboard.h"
#include <iostream>
#include "joc.h"

/*
void Keyboard::HandeEventPlayer(SDL_Event event,Jucator* x, const Uint8* keys1, ObiectJoc* glont)
{
	
	
	while(SDL_PollEvent(&event) != 0)
	{
		
		if (keys1[SDL_SCANCODE_D])
		{	
			
			x->Control((char*)"D");
		}
		
		if (keys1[SDL_SCANCODE_W])
		{
			
			x->Control((char*)"W");
		}
		if (keys1[SDL_SCANCODE_A])
		{
			
			x->Control((char*)"A");
		}
		if (keys1[SDL_SCANCODE_S])
		{
			
			x->Control((char*)"S");
		}

		
	}

	

}
*/

